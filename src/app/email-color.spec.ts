import { EmailColor } from './email-color';

describe('EmailColor', () => {
  it('should create an instance', () => {
    const directive = new EmailColor();
    expect(directive).toBeTruthy();
  });
});
